if ('<%= user_signed_in? %>' == 'true'){
	$('.thumbnail_event_<%=@event.id %> .status').html("<%=j render partial: 'users/event_box_buttons',  locals: { event: @event } %>");
}
else {
	$('.thumbnail_event_<%=@event.id %> .status').html("<%=j link_to 'Faça login primeiro', new_user_session_path, class: 'btn btn-default' %>");
}